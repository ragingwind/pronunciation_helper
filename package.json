{
  "name": "pronunciation-helper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "English chunk-reading & pronunciation practice — a static single-page app.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "deploy": "node scripts/deploy.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
